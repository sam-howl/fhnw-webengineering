package myapp

import grails.rest.Resource

class RecipeIngredient {
    //would be replaced with 1 to many connection
    //static belongsTo = [recipe: Recipe, ingredient: Ingredient]
    //Double amount

    static constraints = {
    }
}
